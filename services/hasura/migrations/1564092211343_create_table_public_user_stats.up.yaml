- args:
    sql:
      CREATE TABLE "public"."user_stats"("user_stats_id" integer NOT NULL, "time_spent"
      integer NOT NULL DEFAULT 0, "total_completed" integer NOT NULL DEFAULT 0, PRIMARY
      KEY ("user_stats_id") );
  type: run_sql
- args:
    name: user_stats
    schema: public
  type: add_existing_table_or_view
