- args:
    sql:
      CREATE TABLE "public"."content_type"("content_type_id" integer NOT NULL,
      "name" text NOT NULL, PRIMARY KEY ("content_type_id") , UNIQUE ("content_type_id"),
      UNIQUE ("name"));
  type: run_sql
- args:
    name: content_type
    schema: public
  type: add_existing_table_or_view
